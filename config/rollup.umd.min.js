import uglify from 'rollup-plugin-uglify';
import config from './rollup.umd';

config.dest = 'dist/wordup.min.js';
config.plugins.push(uglify({
  output: { comments: /\/\*!.*\*\// }
}));

export default config;
